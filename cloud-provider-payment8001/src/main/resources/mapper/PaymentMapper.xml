<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.springcloud.dao.PaymentDao">

    <resultMap id="PaymentMap" type="com.atguigu.springcloud.entities.Payment">
        <id column="id" property="id" javaType="bigint"/>
        <result column="serial" property="serial" javaType="varchar"/>
    </resultMap>

    <insert id="create" parameterType="Payment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment(SERIAL)
        VALUES (${serial});
    </insert>

    <select id="getPaymentById" parameterType="long" resultMap="PaymentMap">
        SELECT *
        FROM payment
        WHERE id = #{id};
    </select>

</mapper>